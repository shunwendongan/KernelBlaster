# Minimal runtime for the untrusted GPU worker. PyTorch/CUDA are supplied by
# the pinned nvcr.io/nvidia/pytorch:25.01-py3 base image.
fastapi>=0.115,<1
loguru==0.7.3
psutil>=6,<8
pydantic>=2.10,<3
python-dotenv>=1,<2
python-multipart==0.0.20
uvicorn[standard]>=0.34,<1
