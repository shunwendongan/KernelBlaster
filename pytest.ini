[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    gpu: requires an NVIDIA GPU and CUDA toolchain
    gpu_sandbox: requires an NVIDIA GPU, Docker Engine, and a pinned sandbox image
    live_api: sends a bounded request to a configured external API
